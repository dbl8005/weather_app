class AppAnimations {
  // Durations
  static const Duration shortDuration = Duration(milliseconds: 150);
  static const Duration mediumDuration = Duration(milliseconds: 300);
  static const Duration longDuration = Duration(milliseconds: 500);

  // Page transitions
  static const Duration pageTransitionDuration = Duration(milliseconds: 300);

  // Button animations
  static const Duration buttonPressDuration = Duration(milliseconds: 100);
  static const Duration buttonPopDuration = Duration(milliseconds: 150);

  // Loading animations
  static const Duration loadingSpinnerDuration = Duration(milliseconds: 1500);
  static const Duration shimmerDuration = Duration(milliseconds: 1000);
}
